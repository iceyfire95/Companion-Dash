#!/bin/sh
set -e

if [ -d /run/systemd/system ] && command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
fi

# User data in $HOME/.companion-web-dashboard is intentionally preserved.

exit 0
